# Residual failure audit

Audit before implementation, 2026-09-10. Baseline `097f57d`; branch `fix/pld-libero-residual`. Scope: D0 only, no distillation. Old results/artifacts are not overwritten. Reported historical D0 21/50 → 0/50 is evidence of failure, not evidence identifying a unique cause; old binary artifacts are absent on this new machine.

References: [PLD v1, Algorithms A and Appendices B/C](https://arxiv.org/html/2511.00091v1), [author project](https://wenlixiao.com/self-improve-VLA-PLD), [SERL](https://github.com/rail-berkeley/serl/tree/1fa2af7496be042e43d36120a7c8d1b71f1bfb59), [Cal-QL reference](https://github.com/nakamotoo/Cal-QL), pinned OpenPI submodule. The author project links paper/videos, not a public PLD training repository; searches did not locate one. SERL is a cited implementation foundation, **not verified author PLD code**. Exact unpublished choices remain unknown.

Line numbers below refer to baseline revision and are preserved by these [source links](https://github.com/qrvmil/maniskill_myws/tree/097f57d/src/maniskill_myws/pld).

| Issue / verdict | Current implementation | PLD/reference implementation | Severity | Likely effect | Planned fix |
|---|---|---|---|---|---|
| 1. Visual initialization — **CONFIRMED MISMATCH** | `libero_experiment.train:216–222` constructs random encoder; no weight load. `sac.ResNetV1_10Encoder:107–143`: 32/64/128/256 channels, 8-group GN, cameras channel-stacked, /255 only. `load_visual_encoder:909` silently accepts partial/zero matches. | PLD C.1: pretrained ResNetV1-10 actor/critic. SERL `vision/resnet_v1.py`: 64/128/256/512, GN4, ImageNet normalization, per-camera encoding, frozen convolutional trunk, learned spatial pooling. SERL publishes `resnet10_params.pkl` trained on ImageNet-1K. | Critical fidelity blocker | Sparse RL must learn vision from scratch; old architecture cannot directly load reference tensors. Causality for collapse unproven. | Add explicit SERL-compatible option; exact tensor mapping and strict full-trunk load for all five networks, weights SHA and JAX/PyTorch feature parity. Keep legacy architecture loadable. No arbitrary substitute model. |
| 2. Train vs eval — **CONFIRMED MISMATCH** | `train.residual:245` calls sampled OTF with base fallback; `evaluate:139` always uses `actor_residual`, deterministic mean. | PLD B.2: OTF samples residuals about fixed base, default 1. Explicit unedited fallback is not specified. Deterministic mean is a different deployment policy, not an OTF equivalent. | Critical interpretation / plausible collapse contributor | Training can avoid actor edits; deterministic evaluation cannot. | Explicit deterministic_actor and otf modes; same shared OTF selector/config at rollout and evaluation, isolated seeded RNG; record candidates/base choice. Preserve fallback as declared extension, configurable. |
| 3. Actor updates in warmup — **PLAUSIBLE ISSUE** | `train.on_transition:250–255` unconditionally calls SAC; `sac.update:678` updates actor/alpha every two updates even during 100 base-only episodes. | PLD Algorithm 1 has an unconditional actor update after its warmup rollout branch. No explicit freeze. SERL supports critic-only updates but does not prove authors used them here. | High hypothesis, **not confirmed reference bug** | Actor may exploit inaccurate Q before own interaction. | Configurable actor/alpha freeze, OFF updates as requested safety baseline; disclose deviation from literal pseudocode. Programmatically compare actor/alpha before/after, assert critic changed. |
| 4. Debug scale — **CONFIRMED MISMATCH** | Config batch 8, replay 10k, 50k total including warmup; 1 update/step. | PLD Table 5: batch 256, replay 250k, gamma .99, clip norm 1, lr 3e-4, AdamW, 100 warmup episodes, critic:actor 2, OTF 1, initial temperature 1, tau .005. Fixed 250k interactions and exact UTD are not specified in Table 5; async synchronization cadence is not UTD. | High | Small batches/noisy critic, limited active experience; not paper-scale. | Separate V2 config batch256/replay250k, explicit update ratio and ACTIVE step budget (5k sanity, 250k only after gates). Measure VRAM before scaling; do not silently shrink. |
| 5. Base alignment — **CONFIRMED MISMATCH** (undertraining **PLAUSIBLE ISSUE**) | `make_openpi_config:80–91` hardcodes batch1; 3000 updates/5832 pairs ≈0.514 sampled data-equivalent epochs. Full torch SFT; single final manifest, no validation selection tooling. | PLD C.2: rank32 LoRA, 8 L40, upstream defaults. Pinned OpenPI defaults 30k updates; its low-memory config actually uses VLM rank16/action-expert rank32. | High | Weak base priors, misleading fixed-budget selection. More training not guaranteed better. | Configurable SFT batch/checkpoint cadence; D0-only checkpoint manifests/selection. Prefer official JAX LoRA with explicit rank32 override if feasible; otherwise justify measured full-torch alternative. Cannot resume absent old binaries: rebuild source-only alignment. |
| 6a. Entropy target / sign — **NOT AN ISSUE** in main OTF coordinates | Scaled tanh density; configured Htarget = -3.5+7 log(.5); alpha loss `-log_alpha*(logp+Htarget)` (`sac:693`). | PLD target -act_dim/2, coordinates unspecified. H(scale X)=H(X)+d log(scale). SERL optimizes softplus temperature times H-Htarget; old code uses log-alpha surrogate and AdamW. | Medium fidelity uncertainty | No demonstrated sign bug. Different optimizer parameterization can change temperature dynamics. | Keep explicit coordinate convention, test gradient direction and stable exact tanh Jacobian. Record alpha/entropy. Do not claim exact PLD temperature implementation. |
| 6b. Cal-QL — **NOT AN ISSUE** for bound/indexing; **PLAUSIBLE ISSUE** for clipped density | `_calql_penalty:729`: floor current and next policy Q by current-state MC, evaluate next-state samples at current states, do not floor uniform random Q. Uses residual pre-clipping logp as executed-action importance density. Critic-only pretrain samples a fixed random residual actor; no auxiliary offline actor. | Cal-QL `JaxCQL/conservative_sac.py:213–270` confirms bound and indexing. Clipping adds probability mass at action limits; shifted bounded residual also lacks global support. Standalone Cal-QL also trains its actor (`conservative_sac.py:163,324–327`); PLD Algorithm1 initializes critic before random residual actor but leaves proposal/auxiliary actor unspecified. Critic-only adaptation is a confirmed standalone-reference difference, not a proven PLD mismatch. | Medium | Importance correction near saturated base actions is not exact; Q(base)>MC is not automatically calibration error because future policy differs. | Preserve reference-style bound; expose conservative knobs; document density approximation, report clipping and Q/MC distributions. Detach policy sampling during critic-only pretrain to avoid useless actor graphs. |
| 6c. OTF/target cadence — **CONFIRMED MISMATCH** for Polyak cadence | OTF min-twin argmax, hard target without entropy, explicit base candidate (`sac:406–452,614`). Polyak only inside actor-update branch (`sac:703`). | SERL SAC updates targets on every critic update (`agents/continuous/sac.py:284`). PLD pseudocode likewise updates targets each RL step; fallback/entropy details not fully specified. | High when adding warmup freeze | Naive actor freeze also freezes targets; old targets track at half intended cadence. | Target updates independent of actor updates; test critic-only target motion and exact OTF train/eval semantics. |
| 6d. Terminals/clipping — **NOT AN ISSUE** for stated finite horizon; additional **CONFIRMED BUG** at budget cutoff | `LiberoEnv.step:152`: success vs timeout distinct; runner combines for finite-horizon mask. train shortens env.horizon to remaining budget, making administrative cutoff a failure terminal. Action bounded once. | Finite-horizon terminal reward ≤1; PLD timeout convention unspecified. Budget exhaustion is not a task terminal. | Medium | Last shortened rollout introduces false terminal transition; unit-residual bounds themselves correct. | Stop on whole episodes, report small active-budget overshoot; retain true task horizon and reset/seed contracts. |

Additional blocking workflow defect: `require_specialist_regimen` rejects every intermediate checkpoint because training_steps must equal online_steps. This prevents requested D0 validation/checkpoint selection. Permit explicitly scoped source validation of incomplete budgets, retain exact recorded training regimen and require selected-checkpoint evidence for final evaluation. Never relabel a short checkpoint as a completed main budget.

Hardware: NVIDIA A100-SXM4-80GB, driver 580.159.03, system CUDA12.8, torch2.11.0+cu128. Container RAM limit 259482714112 bytes (~241.7 GiB), disk237 GiB initially free. Pinned experiment venv is being installed separately. Replay250k with two128px uint8 cameras, current+next, costs45.8 GiB for images, feasible in RAM. Cal-QL currently repeats vision per action candidate; batch256×10 needs a feature-reuse optimization/measurement. Workspace is not volume-backed; no remote push authorized.

Additional **CONFIRMED BUG (reproducibility)** found during Gate2 controls: `AlignedOpenPIModel` did not constrain GPU compiler autotuning. Identical raw images/proprioception and explicit noise produced action chunks differing by0.020766 across two fresh JAX0.5.3 processes, while repeated inference inside each process was exact. Two fresh processes with `--xla_gpu_autotune_level=0` match exactly, without changing weights, bfloat16, or transforms. This is consistent with [XLA compilation-time nondeterminism](https://openxla.org/xla/determinism). A versioned numerical contract now fixes the flag/backend/runtime, changes the execution hash, rejects conflicting settings, and records flags. Old warmup run is stopped and preserved; base selection/zero/replay/warmup must be rerun under the canonical contract. Raw evidence: `V2-audit/provenance/base_inference_repro.json`.

## Execution plan and gates

Use existing adapters/replay/SAC; local targeted changes. User has already authorized implementation and D0 experiments. Tests first for each changed contract. Raw commands, reference snapshots, dependency logs and measurements live in `outputs/pld_libero/V2-*`.

- [x] Strict reference visual weights and cross-framework parity; preserve old checkpoint compatibility.
- [x] Critic-only update control, independent Polyak, stable density and diagnostics; regression tests.
- [x] Shared rollout/eval modes, intermediate D0 validation, independent active budget and provenance; tests.
- [x] Source-only SFT settings/checkpoint selection; four candidates evaluated only on seeds2000–2019, selected4000 (17/20).
- [x] Gate1: selected base vs zero, all20 pairs identical actions/states/images/length/success (max error0).
- [ ] Gate2: real base warmup actor/alpha identity, critic motion, Q/MC/OOD diagnostic.
- [ ] Gate3: 5k–10k active D0 steps; base/deterministic/OTF paired validation with intermediate checkpoints.
- [ ] Only if gates pass: main source run, select by D0 validation, final D0 seeds3000–3049 once. No D1–D5 in current execution plan.

Final seeds were already used by the historical failed pipeline. They remain unchanged, and must not influence V2 selection; they cannot honestly be described as globally never observed. V2 final evaluation will be held out from V2 decisions.

Implementation review: independent CPU review identified episode-budget validation and selection provenance gaps; each was reproduced and regression-tested. Selection now revalidates every ordered candidate. All raw review/test evidence is in V2-audit; environment gates remain open work, not implied by unit tests.
