# V3 execution plan and preregistration

User-approved scope: working source residual, then direct frozen D0–D3 transfer; no distillation or push. Branch `fix/pld-libero-residual-v3`.

1. Complete reference audit before changes; preserve findings in RESIDUAL_V3_AUDIT.md and raw source evidence in V3-audit.
2. Add failing contract tests, then local compatible fixes: uniform std/unit density, shared encoder/stop-gradient, mean actor Q, independent LR warmup, auxiliary Cal-QL actor, probing exclusion and counts, scale scheduler, resumable state.
3. Fix primary base **SFT3001** before any V3 residual result (canonical V2 D0 validation 14/20); secondary **SFT4000** (18/20). Rebind verified source-only manifests to preregistered V3 held-out seeds without changing weights, source/tasks/train seeds/normalization. Reproduce moderate base and zero equivalence, collect 50 successful source base trajectories.
4. V3 D0 validation **2000–2049**; interim checkpoints use first20, finalist comparison all50. Fresh final D0–D3 reset block **4000–4049**, first20 exploratory, extend same setup to50 if feasible. No previous V3 results exist at registration. Original D0–D5 task definitions remain unchanged; only D0–D3 evaluated.
5. Primary A: uniform std, shared SERL frozen trunk/trainable critic visual heads, actor mean Q, 2000-step LR warmup, auxiliary full-action Cal-QL, xi=.5, 100 base-only warmup episodes with actor/alpha frozen, batch256, replay250k, 50:50 replay, OTF one edit plus exact base. Cal-QL1000 updates is inherited experimental budget, not a paper-exact value. Actor/critic AdamW weight decay remains explicitly .01; online temperature uses verified SERL softplus multiplier objective (initial alpha1, no weight decay). Auxiliary Cal-QL uses reference Adam actor/temperature1e-4, critic3e-4, log-alpha objective, target entropy -7; its optimizer state is discarded at handoff so online LR warmup begins at zero. The visual/MLP backbone stays compatible with the residual critic rather than the state-only Cal-QL benchmark architecture.
6. Probing ablation preregistration: primary uniform integer T_probe in [0,floor(.3*horizon)] per active episode. Fractions 0 and .6 are bounded D0-only alternatives only if primary fails to obtain a positive D0 validation gain after50k. Prefix consumes original horizon, never replay/update; prefix-only success separate from residual success. Evaluation deploys from reset, no probing. These fractions are our settings, not published author values.
7. B only if sampled corrections remain destructive: xi .2→.5 linearly over25k ACTIVE steps, same unit density/entropy target -3.5. C only if auxiliary Cal-QL ranking remains poor: compare legacy critic-only initialization and a longer auxiliary budget (5000), D0 only. No candidate count increase.
8. Counterfactual test before trusting OTF: fixed D0 train seeds including successes/failures; replay exact executed prefix from reset (reconstructs controller, simulator and base cache/RNG without incomplete state cloning), branch one base/deterministic/small/large edit, continue frozen base to horizon. Verify repeated base branch equivalence. Report discounted outcome ranks, ties, FPR/FNR, margins, bootstrap uncertainty; this measures one-step intervention followed by base, not full residual-policy Q. Stop only persistent strong empirical ranking contradiction, nonfinite/exploding learning, saturation, or contract violation.
9. Run ≥50k active interactions, checkpoints5/10/25/50k, then100/250k if recovering/useful. Preserve optimizer/replay/RNG at stage boundaries. No stopping solely on5k SR. Select checkpoint and deployment (deterministic or exact OTF) by D0 validation paired SR; ties prefer smaller mean executed correction, then earlier checkpoint. Require residual SR > base SR for positive source finding; report paired uncertainty, not significance from one success.
10. Freeze base/residual hashes, deployment, scale and all numerical settings before any D1–D3 outcomes. Evaluate every task, paired reset states, first20 then optionally50. No target tuning. Save rescue/harm paired videos and concise RESULTS_V3.md; raw commands/provenance in artifacts. Run full tests and independent code review before long training.
